<p><%= @name %>의 선택은</p>
<%= image_tag @img %>


<div class="container">
  <div class="row">
    <% (1..20).to_a.each do |idx| %>
      <div class="col-4"><%= image_tag "#{idx}" %></div>
    <% end %>
  </div>
</div>
